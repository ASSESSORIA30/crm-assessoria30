[phases.build]
cmds = ["npx prisma generate --schema=./prisma/schema.prisma", "npx nest build"]

[start]
cmd = "npx prisma db push --schema=./prisma/schema.prisma --skip-generate --accept-data-loss && node dist/src/main.js"
