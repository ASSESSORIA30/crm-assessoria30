[build]
builder = "nixpacks"
buildCommand = "cd apps/api && npm install && npx prisma generate --schema=./prisma/schema.prisma && npx nest build"

[deploy]
startCommand = "cd apps/api && node dist/main.js"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
